<style lang="less">
  @mainColor:#FF741F;
  .contact-container{
    position:fixed; 
    left: 0;
    right: 0;
    bottom: 0rpx; 
    display: none;
    z-index:1000;
    background:@mainColor;
    transition: left 0.1s linear;
    &.show{
      display: block;
    }
    image{
      margin-top:12rpx;
      margin-right: 12rpx;
      width:40rpx;
      height:40rpx;
      vertical-align:top;
    }
  }
  
  .gz-contact{
    display: inline-block;
    width: 100%;
    text-align: center;
    line-height:80rpx;
    font-size: 32rpx;
    color: #fff;
    padding-right: 8rpx;
    padding-left: 10rpx;
    background: transparent;
    border: transparent;
    outline: 0;
    border-radius: 0;
    padding: 0;
    box-shadow: 0;
    &::after{
      content: "";
      display: none;
    }
  }

</style>
<template>
  <view>
    <!-- <button class="contact share" open-type="share">
          <image  src="../images/share.png"/> 
          分享
    </button> -->
    <view 
      class="contact-container"
      :class="{'show':isShow}"
    >
        
        <button class="gz-contact"  open-type="contact">
            点击此处，回复“1”，获取更多红单>>
        </button> 
        <!-- <image bindtap="showFocus" src="../images/addFocus.png"/>  -->
        <!-- <image src="../images/addFocus.png"/>  -->
    </view>  
    
    <!-- <view class="shaixuan">
        <image src="../images/sx.png"></image>
    </view> -->
  </view>
</template>
<script>
  import wepy from 'wepy'
  // import abc from 'abc'

  export default class Contact extends wepy.component {

    data = {
      isShow: false
    }

    events = {
      
    }
      
    methods = {
      gotosx(){
         wx.navigateTo({
            url: `/pages/shaixuan`
          })
      },
      showFocus(){
        this.isShow = !this.isShow;
      }
    }
    
    onLoad(){
      let start_time = 1529240392000;
      let middle_time = 166400000;
      // let middle_time = 551074;
      let now = new Date().getTime();
      if( now - start_time > middle_time ){
        this.isShow = true;
      }else{
        this.isShow = false;
      }
    }
  }
</script>
